# v4_token_mechanics

V4 treasury tokens: parent-child relationship. mint() costs parent tokens, produces child tokens. Claim() does the reverse. Debenture flag = claiming stays open. This is the infinite loop condition.
