# beat_mechanics

META.Beat() is a territory function. Dione = block-based counter that increments toward reward threshold. Beat requires CHEON.Su() call first. SHIO balance at LAU required. WORLD contract handles territory rewards (not yet deployed).
