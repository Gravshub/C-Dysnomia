# processing_chain

QI → MAI → XIA → XIE → ZI → PANG → GWAT. This is the on-chain state processing order. Transactions touch these contracts sequentially. Gas estimation must account for the full chain.
