# shio_pair_system

Each token has a SHIO pair: Rod (accumulated) and Cone (emitted). Rho() function cycles Cone → Rod. Upsilon = the Rho reward. Pi = the Cone emission. Alpha = first-minted. Beta = second. These are the LAU engine cycle states.
